/* ============================================================================
   This file contains the configurations for sonarqube
   ============================================================================

   Included from: "${rootProject.projectDir}/build.gradle"
   ============================================================================
 */
def excludePatterns = [
        "**/domain/**/*.java",
        "**/configuration/**/*.java",
]

sonarqube {
    properties {
        property "sonar.sources", "src/main/java/cl"
        property "sonar.exclusions", excludePatterns
    }
}


